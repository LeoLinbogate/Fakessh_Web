// src/store/index.js
import { createStore } from "vuex";

export default createStore({
  state: {
    // 这里定义你的状态
  },
  mutations: {
    // 这里定义改变状态的方法
  },
  actions: {
    // 这里定义异步操作
  },
  getters: {
    // 这里定义获取状态的方法
  },
});
